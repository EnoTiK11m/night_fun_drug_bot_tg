services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/bot_data.db
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
